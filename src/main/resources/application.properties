spring.application.name=AIService

# OpenAI Configuration
openai.api.key=${OPENAI_API_KEY}

# User Service Configuration (defaults keep app booting if secrets not injected)
user.service.base-url=${USER_SERVICE_BASE_URL:http://localhost:8081}

# Account Service Configuration
account.service.base-url=${ACCOUNT_SERVICE_BASE_URL:http://localhost:8081}

# Crypto Service Configuration
crypto.service.base-url=${CRYPTO_SERVICE_BASE_URL:http://localhost:8081}
